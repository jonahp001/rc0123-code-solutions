/* exported setValue */

/*

  define a function 'setValue' that takes three parameters: 'object', 'key', and value
    in 'object', add the 'key'/ 'value' pair

*/

function setValue(object, key, value) {
  object[key] = value;
}

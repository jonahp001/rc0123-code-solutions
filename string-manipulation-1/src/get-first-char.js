/* exported getFirstChar */

/*

  define a function 'getFirstChar' that takes one parameter, 'string'
    return the value of the first index of 'string'

*/

function getFirstChar(string) {
  return string[0];
}
